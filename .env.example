# Copy this file to .env. It holds only the secret token.
# Every other setting (rootDir, port, readOnly, jsonResponse) lives in config.json.

# Leave this empty on the first run - the server generates a token and writes it here.
MCP_TOKEN=
